[package]
name = "vc-core"
version = "0.1.0"
edition = "2021"
description = "Domain core for vibe-control work context switching app"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
thiserror = "1.0"

[dev-dependencies]
proptest = "1.0"
