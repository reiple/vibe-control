# 작업 맥락 전환 데스크톱 앱 요구사항서

문서 버전: 1.3  
대상 운영체제: macOS, Windows

> 변경 이력:
> - 1.3 (2026-09-09) — §13.7 추가(크로스플랫폼 무프레임 창 — 윈도우 제목표시줄 제거, 맥과 동일 화면).
> - 1.2 (2026-09-08) — §13.5 추가(실행 중인 프로세스 아이콘 고해상도 표시 명확화).
> - 1.1 (2026-09-08) — §13 추가 명확화(실행 앱 목록·컨텍스트 그룹·드래그·더블 클릭) 추가.

## 1. 목적

사용자가 여러 프로젝트를 오갈 때 필요한 앱, 창, 브라우저 탭, 폴더를 하나의 작업 묶음으로 저장하고 다시 불러올 수 있는 데스크톱 앱을 개발한다.

사용자는 매번 여러 앱을 찾아 열거나 창을 정리하지 않고, 저장한 작업 묶음을 선택해 이전 작업 환경으로 빠르게 돌아갈 수 있어야 한다.

## 2. 주요 사용자

- 여러 프로젝트를 동시에 진행하는 개발자와 디자이너
- 브라우저, 편집기, 터미널, 파일 관리자를 함께 사용하는 사용자
- 업무별 창 구성을 반복해서 열고 전환하는 사용자

## 3. 핵심 사용 흐름

1. 사용자가 새로운 작업 묶음을 만든다.
2. 현재 실행 중인 창과 브라우저 탭을 목록에서 확인한다.
3. 필요한 항목을 드래그하거나 선택하여 작업 묶음에 추가한다.
4. 다른 작업을 수행하거나 등록했던 창을 닫는다.
5. 저장한 항목 하나를 열거나 작업 묶음 전체를 활성화한다.
6. 열려 있는 항목은 앞으로 가져오고, 닫힌 항목은 저장된 위치나 주소로 다시 연다.

## 4. 기능 요구사항

### 4.1 작업 묶음 관리

- 사용자는 작업 묶음을 생성하고 이름을 변경하고 삭제할 수 있어야 한다.
- 작업 묶음에는 여러 개의 리소스를 저장할 수 있어야 한다.
- 작업 묶음을 삭제하기 전에는 확인 절차를 제공해야 한다.
- 작업 묶음과 리소스는 앱을 종료하고 다시 실행해도 유지되어야 한다.

### 4.2 실행 중인 항목 표시

- 현재 실행 중인 일반 앱 창과 브라우저 탭을 왼쪽 패널에 표시해야 한다.
- 같은 앱의 창이 여러 개이면 앱 아이콘은 한 번만 표시하고 창 제목을 그 아래에 나열해야 한다.
- 각 앱은 운영체제에서 사용하는 실제 앱 아이콘을 표시해야 한다.
- 현재 선택된 창은 여러 창이 있는 그룹 안에서만 녹색 점으로 구분해야 한다.
- 검색어로 실행 중인 항목을 필터링할 수 있어야 한다.
- 항목을 더블 클릭하면 해당 창이나 탭을 가장 앞으로 가져와야 한다.
- 이미 닫힌 창, 시스템 보조 창, 중복된 브라우저 탭이 목록에 남아서는 안 된다.

### 4.3 리소스 등록

- 실행 중인 항목을 작업 묶음으로 드래그하여 추가할 수 있어야 한다.
- 작업 묶음의 추가 버튼을 통해 실행 중인 창을 선택할 수 있어야 한다.
- 파일, 폴더, 앱 실행 파일, 웹 주소를 직접 끌어다 놓아 추가할 수 있어야 한다.
- 같은 작업 묶음에 동일한 창을 중복 등록하지 않아야 한다.
- 같은 앱의 서로 다른 창, 경로가 다른 폴더, 같은 주소를 연 서로 다른 탭은 별도 항목으로 등록할 수 있어야 한다.

### 4.4 개별 항목 활성화

- 저장된 항목을 선택하면 정확히 그 창이나 브라우저 탭을 앞으로 가져와야 한다.
- 앱만 활성화하고 다른 창을 보여주는 동작은 성공으로 처리하지 않아야 한다.
- 등록된 창이 닫혀 있으면 가능한 경우 저장된 문서, 폴더 또는 웹 주소를 다시 열어야 한다.
- 다시 열린 창을 발견하면 이후에는 해당 창을 같은 항목으로 추적해야 한다.

### 4.5 작업 묶음 전체 활성화

- 사용자는 한 번의 동작으로 작업 묶음의 모든 항목을 활성화할 수 있어야 한다.
- 항목은 저장된 순서대로 처리해야 한다.
- 일부 항목이 실패해도 나머지 항목은 계속 처리해야 한다.
- 완료 후 성공, 찾지 못한 항목, 오류를 사용자에게 알려야 한다.

### 4.6 항목 편집

- 사용자는 항목의 표시 이름을 변경할 수 있어야 한다.
- 사용자는 닫힌 항목을 다시 열 때 사용할 웹 주소나 경로를 수정할 수 있어야 한다.
- 표시 이름을 변경해도 실제 창을 식별하는 정보는 바뀌지 않아야 한다.
- 브라우저 탭은 등록 당시의 전체 주소를 보관해야 하며, 이후 다른 페이지로 이동했다고 해서 저장된 재실행 주소가 자동으로 바뀌어서는 안 된다.

### 4.7 상태 표시와 갱신

- 저장된 항목은 활성, 비활성, 권한 필요 상태로 구분해야 한다.
- 상태는 항목 아이콘 왼쪽의 작은 원으로 표시해야 한다.
- 비활성 항목은 아이콘과 글자 색을 흐리게 표시해야 한다.
- 실행 중인 항목과 저장된 항목의 상태는 주기적으로 자동 갱신해야 한다.
- 앱이 화면에 보이지 않거나 창 크기를 조절하는 동안에는 불필요한 반복 조회를 줄여야 한다.
- 갱신 작업이 오래 걸리더라도 같은 작업이 겹쳐 실행되어서는 안 된다.

## 5. 운영체제별 요구사항

### 5.1 macOS

- 실행 중인 앱과 창을 확인하기 위해 시스템의 접근성 기능을 사용해야 한다.
- 최초 실행 시 필요한 접근성 권한과 설정 방법을 안내해야 한다.
- 사용자가 권한을 부여한 뒤 앱에서 실제 권한 상태를 다시 확인할 수 있어야 한다.
- 권한 화면에는 최종 배포 앱의 이름과 식별자가 일관되게 표시되어야 한다.
- 파인더 창은 전체 경로를 내부에 저장하되 화면에는 마지막 폴더 이름을 표시해야 한다.
- 파인더 창을 닫은 뒤 다시 실행하면 등록했던 폴더를 열어야 한다.
- 코드 편집기는 등록한 작업 폴더를 새 창으로 다시 열 수 있어야 한다.
- 사파리와 크롬은 등록한 페이지 주소를 다시 열 수 있어야 한다.
- 일반 앱 창을 활성화할 때 브라우저 탭 탐색을 수행하여 응답이 지연되지 않도록 해야 한다.

### 5.2 Windows

- 별도의 접근성 권한 요청 없이 사용자에게 보이는 최상위 창을 탐색해야 한다.
- 최소화되거나 최대화된 창을 활성화할 때 기존 창 상태를 가능한 한 보존해야 한다.
- 파일 탐색기 창은 등록한 폴더 경로를 다시 열 수 있어야 한다.
- 코드 편집기와 지원 가능한 터미널은 등록 당시의 작업 폴더를 복원해야 한다.
- 브라우저 탭은 운영체제가 제공하는 자동화 식별자를 사용해 서로 구분해야 한다.
- 앱 창을 닫아도 백그라운드에서 실행을 유지하고, 시스템 트레이나 전역 단축키로 다시 표시할 수 있어야 한다.

## 6. 브라우저 요구사항

- 브라우저 탭의 제목과 주소를 함께 수집해야 한다.
- 같은 주소의 탭이 여러 개 있어도 각각 별도의 탭으로 추적해야 한다.
- 등록한 탭을 닫은 뒤 다시 열 때 경로, 검색 문자열, 포트 번호를 포함한 전체 주소를 사용해야 한다.
- 주소에 명시된 HTTP 또는 HTTPS 방식은 그대로 유지해야 한다.
- 로컬 주소와 사설 네트워크 주소는 개발 서버 사용을 고려해 HTTP 주소로 처리할 수 있어야 한다.
- 탭 음소거 버튼, 탭 목록 버튼, 설정 창 등을 실제 웹페이지 탭으로 오인하지 않아야 한다.
- 운영체제가 백그라운드 탭 주소를 제공하지 않는 경우에는 확보하지 못한 주소를 임의로 추측하지 않아야 한다.

## 7. 화면 요구사항

- 화면은 어두운 색상의 데스크톱 대시보드 형태로 구성한다.
- 왼쪽에는 실행 중인 항목 패널을 항상 표시하고, 오른쪽에는 앱 제목과 작업 묶음 카드를 배치한다.
- 별도의 내비게이션 메뉴는 두지 않는다.
- 실행 중인 항목 패널의 너비는 사용자가 드래그해서 조절할 수 있어야 한다.
- 작업 묶음 카드는 넓은 화면에서 두 열, 좁은 화면에서 한 열로 표시해야 한다.
- 작업 묶음 카드의 높이는 사용자가 조절할 수 있어야 한다.
- 앱 자체의 상단 영역을 드래그하여 창을 이동할 수 있어야 한다.
- 모든 아이콘은 고해상도 화면에서 흐릿해 보이지 않아야 한다.
- 작업 묶음 전체 실행 버튼의 문구는 모든 항목을 활성화한다는 의미가 분명해야 한다.
- 상태를 설명하는 불필요한 기술 용어와 반복 정보는 화면에 노출하지 않는다.

## 8. 데이터 요구사항

- 모든 데이터는 기본적으로 사용자 컴퓨터에 저장해야 한다.
- 작업 묶음은 고유 식별자, 이름, 리소스 목록을 가져야 한다.
- 리소스는 종류, 표시 이름, 앱 정보, 창 식별 정보, 실행 중 연결 정보, 재실행 주소 또는 경로를 저장할 수 있어야 한다.
- 프로세스 번호와 창 번호는 현재 실행 중인 항목을 찾는 보조 정보로 사용하고 영구적인 식별자로 간주하지 않아야 한다.
- 저장 중 오류가 발생하면 기존 파일과 메모리 상태가 서로 달라지지 않아야 한다.
- 저장 파일은 임시 파일을 먼저 작성한 뒤 교체하는 방식으로 손상 가능성을 줄여야 한다.
- 이전 버전 데이터에 일부 필드가 없더라도 가능한 범위에서 읽을 수 있어야 한다.

## 9. 비기능 요구사항

- 지원 환경은 macOS 12 이상과 Windows 10/11로 한다.
- 일반적인 사용 환경에서 상태 갱신이 입력이나 창 조작을 방해하지 않아야 한다.
- 접근성 탐색과 창 열거는 처리 개수와 탐색 깊이에 제한을 두어야 한다.
- 파일 경로와 웹 주소는 문자열로 명령을 조합하지 않고 운영체제 실행 기능의 인자로 전달해야 한다.
- 앱 아이콘은 캐시하여 반복 추출 비용을 줄여야 한다.
- 운영체제별 기능은 각 운영체제의 실제 환경에서 별도로 빌드하고 검증해야 한다.
- 앱은 사용자의 문서 내용을 수집하거나 외부 서버에 작업 묶음 데이터를 전송하지 않아야 한다.

## 10. 범위에서 제외되는 기능

- 창의 위치, 크기, 모니터 배치 복원
- 운영체제 간 경로 자동 변환
- 사용자 계정과 클라우드 동기화
- 협업과 작업 묶음 공유
- 결제, 구독, 라이선스 관리
- 자동 업데이트와 앱 장터 배포
- 운영체제의 기본 앱 전환 단축키 완전 대체

## 11. 인수 조건

다음 시나리오를 macOS와 Windows의 실제 환경에서 확인해야 한다.

1. 작업 묶음을 만들고 항목을 추가한 뒤 앱을 다시 실행해도 내용이 유지된다.
2. 실행 중인 항목을 더블 클릭하면 정확한 창이나 탭이 앞으로 나온다.
3. 실행 중인 항목을 작업 묶음으로 드래그하면 한 번만 등록된다.
4. 같은 앱의 서로 다른 창을 같은 작업 묶음에 등록할 수 있다.
5. 경로가 다른 두 폴더 창을 구분하고 각각 원래 경로로 다시 연다.
6. 서로 다른 작업 폴더를 연 코드 편집기 창을 구분하고 각각 복원한다.
7. 실제 브라우저 탭 수와 실행 중인 항목 목록의 탭 수가 일치한다.
8. 같은 주소의 브라우저 탭 두 개 중 하나를 닫아도 다른 탭으로 잘못 연결되지 않는다.
9. 저장된 브라우저 탭을 닫은 뒤 다시 실행하면 등록한 전체 주소가 열린다.
10. 일반 메신저 대화 창을 활성화할 때 장시간 응답 대기 상태에 머물지 않는다.
11. 작업 묶음 전체 활성화 중 일부 항목이 실패해도 나머지 항목은 처리된다.
12. 작업 묶음을 삭제한 뒤 앱을 재실행해도 삭제된 항목이 다시 나타나지 않는다.
13. 권한이 필요한 운영체제에서는 실제 권한 상태와 앱의 안내가 일치한다.
14. 패널과 카드 크기를 조절한 뒤 다시 실행해도 사용자 설정이 유지된다.
15. 고해상도 화면에서 앱과 브라우저 아이콘이 선명하게 표시된다.

## 12. 완료 기준

- 위 기능 요구사항과 인수 조건을 충족한다.
- macOS와 Windows에서 설치 가능한 빌드 산출물을 생성한다.
- 자동 테스트, 정적 검사, 각 운영체제의 실행 확인을 통과한다.
- 주요 실패 상황에서 앱이 종료되지 않고 사용자에게 원인을 알린다.
- 데이터 저장 위치, 권한 설정 방법, 기본 사용법을 사용자 문서로 제공한다.

## 13. 추가 명확화 (2026-09-08)

아래는 사용자가 화면 구성과 상호작용을 구체화한 내용이다. 기존 §4.1~§4.4 및 §7 화면 요구사항을 재확인·구체화하며, 이 문서에서 사용하는 "작업 묶음"은 사용자가 말한 "컨텍스트 그룹"과 동일한 개념이다.

- **13.1 실행 중인 앱 목록** — 앱 화면에는 현재 이 컴퓨터에서 실행 중(떠 있는)인 앱들을 리스트업하는 영역이 상시 존재해야 한다. 목록은 **앱 단위로 그룹화**하되(앱 아이콘은 앱당 한 번만 표시), 그룹은 그 앱의 **개별 창/탭**을 담는 컨테이너다. (§4.2 FR-2.1/2.2 재확인 — 앱 그룹 + 그 아래 창/탭. **개정 2026-09-08**: 초기 "'앱' 단위 목록임을 명시"는 원 요구 FR-2.2/2.4(앱 그룹 안에 창 나열)를 과도하게 앱 단위로 좁힌 것이므로 창 단위를 포함하도록 개정. §13.6 참조.)
- **13.2 컨텍스트 그룹 추가** — 사용자는 컨텍스트 그룹(= 작업 묶음)을 직접 추가·생성할 수 있어야 한다. (§4.1 재확인)
- **13.3 실행 앱 → 컨텍스트 그룹 드래그** — 13.1의 실행 중인 앱(프로세스) 항목을 13.2의 컨텍스트 그룹으로 드래그하여 넣을 수 있어야 한다. (§4.3 구체화 — 프로세스/앱 단위 드래그 앤 드롭)
- **13.4 활성화 상호작용** — **컨텍스트 그룹 패널**에서는 개별 항목을 더블 클릭하면 정확히 그 창/탭을 최전면으로 가져오고, 닫혀 있는 경우 실행(열기)한다(§4.4 개별 항목 활성화). **실행 중인 앱 목록 패널**에서는 앱 항목 클릭이 창/탭 목록 펼침/접기(§13.6)이고, 펼쳐진 개별 창/탭을 선택하면 정확히 그 창/탭만 최전면으로 온다. (§4.2 · §4.4 · §11 인수 조건 2번. **개정 2026-09-08**: 초기 "개별 앱을 더블 클릭 → 앱 최전면"은 원 요구 FR-2.6/FR-4.1(정확한 창/탭 활성화)·FR-4.2(앱만 활성화는 성공 아님)와 배치되어, 왼쪽 패널은 "앱 펼침 → 창 선택 → 정확한 창 활성화" 흐름으로 개정. 실행 중이 아닌 앱을 다시 여는 동작은 창 목록이 비어 있을 때 앱 실행으로 대체.)
- **13.5 프로세스 아이콘 고해상도 표시** — 실행 중인 앱 목록(§13.1)과 컨텍스트 그룹에 등록된 앱 항목 모두, 각 프로세스가 사용하는 실제 앱 아이콘을 **고해상도(Retina 화면에서 흐릿하지 않게)** 로 표시해야 한다. (§4.2 "운영체제에서 사용하는 실제 앱 아이콘 표시" · §7 "모든 아이콘은 고해상도 화면에서 흐릿해 보이지 않아야 한다" 재확인·구체화)
  - 아이콘은 운영체제에서 앱 번들 식별자(가능한 경우) 또는 앱 이름으로 조회한 실제 아이콘을 사용한다.
  - 표시 상자 크기의 최소 2배 이상 해상도의 원본을 사용하여 고해상도 화면에서 선명하게 보여야 한다.
  - 아이콘 추출은 목록 최초 로딩을 지연시키지 않도록 각 항목별로 비동기(지연) 처리하고, 추출 실패나 로딩 중에는 기본 표시(색상 점 등)로 우아하게 대체해야 한다.
  - 한 번 추출한 아이콘은 캐시하여 반복 추출 비용을 줄여야 한다. (§9 비기능 요구사항 "앱 아이콘은 캐시하여 반복 추출 비용을 줄여야 한다" 재확인)

- **13.6 앱 창/탭 펼치기·선택 (신설 2026-09-08)** — 실행 중인 앱 목록(§13.1)에서 앱 항목을 클릭하면 해당 앱의 **현재 실행 중인 창/탭 목록**을 앱 바로 아래에 펼치고(expand), 다시 클릭하면 접는다(collapse).
  - 각 창/탭은 서로 **구분 가능한 제목**(창 제목 / 탭 제목·주소)을 표시한다.
  - 펼쳐진 창/탭을 클릭(선택)하면 §13.4에 따라 **정확히 그 창/탭만** 최전면으로 온다(다른 창을 대신 보여주면 성공으로 처리하지 않는다).
  - 창/탭이 **하나뿐인 앱**도 동일한 상호작용·데이터 모델로 처리한다(펼치면 항목 1개; 앱 클릭만으로 임의 창을 즉시 활성화하지 않는다).
  - **현재 활성 창/탭**은 그 앱 그룹 안에서 기존 상태 표시(녹색 점, §4.2 FR-2.4)로 구분한다.
  - 실행이 종료된 창/탭은 주기 갱신 시 목록에서 **자동 제거**한다.
  - 이 상호작용은 §13.3 드래그 앤 드롭 및 컨텍스트 그룹 기능을 방해하지 않는다. (↔ `aidlc-docs/inception/requirements/requirements.md` FR-2.8, AC-20)

- **13.7 크로스플랫폼 무(無)프레임 창 (신설 2026-09-09)** — 윈도우 버전과 맥 버전 모두 운영체제 기본 **제목표시줄(캡션 바)을 표시하지 않아야** 하며, 두 OS가 **동일한 화면**을 렌더링해야 한다.
  - 맥은 이미 투명 오버레이 제목표시줄(네이티브 신호등 버튼이 콘텐츠 위에 떠 있음)로 무프레임이었다. 윈도우에서는 이 옵션이 macOS 전용이라 무시되어 OS 기본 제목표시줄이 남아 있었다.
  - 윈도우에서 창 장식(decorations)을 비활성화해 제목표시줄을 제거한다. 좌측 실행앱 패널·우측 카드·상단 여백 등 레이아웃은 맥과 동일하게 유지한다(§8 화면 요구사항 재확인).
  - 창 이동은 §8 "상단 영역 드래그로 창 이동" 요구를 그대로 유지하며(상단 드래그 영역), 창 크기 조절도 계속 가능해야 한다.
  - 무장식 창에는 OS 기본 창 버튼이 없으므로, **맥 신호등을 모사한 커스텀 창 컨트롤(좌상단 닫기·최소화·최대화 버튼)을 윈도우에서만** 표시해 맥과 동일한 조작·외형을 제공한다. (↔ `aidlc-docs/inception/requirements/requirements.md` FR-8.11, AC-22)
